# SymBIoT Environment Variables
# Copy this file to .env and fill in your values

# Supabase Configuration
VITE_SUPABASE_PROJECT_ID="your-project-id"
VITE_SUPABASE_PUBLISHABLE_KEY="your-anon-key"
VITE_SUPABASE_URL="https://your-project.supabase.co"

# Google Maps (for location features)
VITE_GOOGLE_MAPS_API_KEY="your-google-maps-api-key"

# App Configuration
VITE_APP_URL="https://your-domain.com"

# Gemini AI (for AI features)
VITE_GEMINI_API_KEY="your-gemini-api-key"

# ===================================
# SUPABASE EDGE FUNCTION SECRETS
# ===================================
# These are NOT set in .env file!
# Instead, configure them in Supabase Dashboard or via CLI:
#
# Via Dashboard:
#   1. Go to Edge Functions → Settings
#   2. Add new secret
#
# Via CLI:
#   supabase secrets set KEY=value
#
# Required Secrets:
# - RESEND_API_KEY: Your Resend API key for sending emails (starts with re_)
#
# These are automatically provided by Supabase:
# - SUPABASE_URL
# - SUPABASE_SERVICE_ROLE_KEY
# - SUPABASE_ANON_KEY

# ===================================
# DATABASE CONFIGURATION
# ===================================
# For the email reports cron job, run these SQL commands:
#
# ALTER DATABASE postgres SET app.supabase_url = 'https://your-project.supabase.co';
# ALTER DATABASE postgres SET app.supabase_service_role_key = 'your-service-role-key';
#
# See docs/EMAIL_REPORTS_SETUP.md for detailed instructions
